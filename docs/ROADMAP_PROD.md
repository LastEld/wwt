# Implementation Plan: Pre-Deployment Activation (ROADMAP)

Этот план описывает шаги по активации реальных интеграций и подготовке платформы WinWin Travel к публичному запуску.

## Proposed Changes

### 📡 Интеграции (Providers)

#### [MODIFY] [hotelbeds-adapter.ts](file:///e:/evidenceWWT/src/integrations/hotelbeds/hotelbeds-adapter.ts)
- Разкомментировать логику `fetch` в методе `search`.
- Реализовать методы `getHotelDetails`, `checkAvailability` и `createBooking` на базе документации Hotelbeds.
- Настроить обработку ошибок (timeouts, rate limits).

### 🧠 ИИ и Данные (AI & Data)

#### [MODIFY] [vector-service.ts](file:///e:/evidenceWWT/src/services/ai/vector-service.ts)
- Интегрировать клиент Pinecone/Weaviate.
- Заменить имитацию `VectorService.search` на реальный векторный поиск.
- Настроить пайплайны обновления эмбеддингов при изменении данных отелей.

### 💳 Транзакции (Finance)

#### [NEW] [stripe-service.ts](file:///e:/evidenceWWT/src/services/transactions/stripe-service.ts)
- Создать сервис для обработки платежей через Stripe.
- Реализовать вебхуки для обновления статуса бронирования.

## Verification Plan

### Automated Tests
- `npm run test:integration` - запуск тестов интеграций с использованием реальных (test) ключей.
- Проверка валидности подписи в Hotelbeds.

### Manual Verification
- Тестирование сквозного процесса бронирования с реальными данными отелей в тестовой среде.
- Верификация появления данных в Vector DB.
