# WinWin Travel: Comprehensive Gap Analysis

Этот документ содержит детальный перечень недоработок и зон развития, выявленных в ходе технического аудита.

## 1. Архитектурные недоработки (Architecture Gaps)

| Область | Описание проблемы | Приоритет |
| :--- | :--- | :--- |
| **Auth** | Demo Mode захардкожен в `auth.config.ts`. Новые пользователи не сохраняются в БД при входе через демо-режим. | High |
| **Price Lock Fallback** | Логика очистки (cleanup) устаревших записей `PriceLock` в Prisma не реализована (Cron job missing). | Medium |
| **Session Management** | `sessionId` в API-роутах часто захардкожен как `"client-session-id"`. Нет реальной привязки к сессии браузера. | Medium |

## 2. Функциональные недоработки (Functional Gaps)

### 🧩 Интеграции
- **Hotelbeds**: Не реализована логика пагинации и фильтрации на стороне провайдера (сейчас все фильтры работают только в памяти после получения данных).
- **Hotel Details**: Метод `getHotelDetails` в адаптерах бросает `Error("Not connected")`. Пользователь не может увидеть подробности отеля.

### 🧠 AI & Intelligence
- **RAG / Vector DB**: `VectorService` использует заглушки. Группировка отелей по темам (`themes`) захардкожена.
- **Neural Learning**: Логика обучения на базе `Reactions` (`ReactionService`) реализована в БД, но не интегрирована в пайплайн переранжирования в реальном времени (нужен фоновый процесс обновления весов).

## 3. Модели Данных (Prisma Gaps)

Следующие модели в `schema.prisma` определены, но **не используются** в коде:
- `PriceHistory`: История цен не записывается.
- `DemandSignal`: Спрос на направления не отслеживается и не анализируется.
- `AgencyPreferences`: Настройки комиссии агентства игнорируются при ранжировании.

## 4. Инфраструктура и Тесты (DevOps Gaps)

- **Тестирование**: В проекте 0 юнит-тестов и интеграционных тестов (директории пусты).
- **Environment**: Отсутствует валидация переменных окружения при запуске (нужен скрипт валидации `.env`).

---

## 🛠️ Рекомендации к выполнению (AMS Pipeline)
Я уже добавил эти пункты в план выполнения (`dry_run` сессия `4d8ba1d1`). Рекомендуется начать с оживления `HotelbedsAdapter` и внедрения реального `sessionId`.
